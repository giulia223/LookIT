@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Resetare cheie autentificare";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="row justify-content-start">
    <div class="col-md-10 col-lg-8">

        <div class="card border-0 shadow-sm rounded-4">
            <div class="card-body p-4">

                @* titlu pagina *@
                <h4 class="fw-bold mb-3 text-danger">@ViewData["Title"]</h4>
                <partial name="_StatusMessage" for="StatusMessage" />

                @* mesaj avertisment *@
                <div class="alert alert-warning border-0 shadow-sm rounded-3 mb-4" role="alert">
                    <div class="d-flex">
                        <i class="bi bi-exclamation-triangle-fill fs-3 me-3 text-warning"></i>
                        <div>
                            <h5 class="alert-heading fw-bold mb-1">Atenție!</h5>
                            <p class="mb-2">
                                <strong>Dacă resetezi cheia de autentificare, aplicația ta curentă nu va mai funcționa.</strong>
                            </p>
                            <p class="mb-0 small text-muted">
                                Acest proces dezactivează autentificarea 2FA până când îți verifici din nou aplicația.
                                Dacă nu finalizezi reconfigurarea, este posibil să pierzi accesul la cont.
                            </p>
                        </div>
                    </div>
                </div>

                <p class="text-muted mb-4">
                    Această acțiune este ireversibilă. Va trebui să scanezi un nou cod QR și să reconfigurezi aplicația Microsoft Authenticator sau Google Authenticator.
                </p>

                @* butoane actiune *@
                <form id="reset-authenticator-form" method="post">
                    <div class="d-flex gap-2">
                        @* buton renuntare *@
                        <a asp-page="./TwoFactorAuthentication" class="btn btn-outline-secondary rounded-pill flex-grow-1 py-2 fw-bold shadow-sm">
                            <i class="bi bi-arrow-left me-2"></i> Anulează
                        </a>

                        @* buton resetare efectiva *@
                        <button id="reset-authenticator-button" class="btn btn-danger rounded-pill flex-grow-1 py-2 fw-bold shadow-sm" type="submit">
                            <i class="bi bi-arrow-counterclockwise me-2"></i> Resetează cheia
                        </button>
                    </div>
                </form>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}